# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Chave de servico (NUNCA expor no client) - usada nas rotas de API e nos scripts
SUPABASE_SERVICE_ROLE_KEY=
# Segredo para assinar a sessao (JWT). Gere com: openssl rand -base64 32
SESSION_SECRET=
